<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.hubert.guide">

    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        <activity android:name=".newbieguide.BieGuideMainActivity" />
        <activity android:name=".newbieguide.TestFragmentActivity" />
        <activity android:name=".newbieguide.GridViewActivity" />
        <activity
            android:name=".newbieguide.FirstActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.hs.act.slbapp.FirstActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".newbieguide.RecyclerViewActivity" />
        <activity android:name=".newbieguide.SimpleGuideViewActivity" />
        <activity android:name=".guideview.newguideview.SimpleGuideViewActivity1" />
        <activity android:name=".newbieguide.ScrollViewActivity" />
    </application>

</manifest>
